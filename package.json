{
  "name": "my-platform",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "turbo": "^1.6.1"
  },
  "engines": {
    "pnpm": ">=7"
  }
}
