{
  "name": "my-platform",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "web": "pnpm -F 'web'",
    "eslint-config": "pnpm -F '@internal/eslint-config'",
    "tsconfig": "pnpm -F '@internal/tsconfig'"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "turbo": "^1.6.1",
    "@internal/eslint-config": "workspace:*"
  },
  "engines": {
    "pnpm": ">=7"
  }
}
