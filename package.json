{
  "name": "my-platform",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "turbo run build",
    "lint": "FORCE_COLOR=1 turbo run lint typechk",
    "lint:ci": "FORCE_COLOR=1 turbo run lint:ci typechk",
    "fmt": "prettier --write .",
    "dev": "turbo run dev",
    "api": "pnpm -F api",
    "web": "pnpm -F web",
    "eslint-config": "pnpm -F @internal/eslint-config",
    "tsconfig": "pnpm -F @internal/tsconfig",
    "types": "pnpm -F @types/internal"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "prettier-plugin-organize-imports": "^3.2.3",
    "prettier-plugin-tailwindcss": "^0.4.1",
    "turbo": "^1.10.9"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=7",
    "npm": "banned",
    "yarn": "banned"
  }
}
