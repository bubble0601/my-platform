{
  "name": "my-platform",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "api",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "api": "yarn workspace @internal/api",
    "web": "yarn workspace web",
    "eslint-config": "yarn workspace @internal/eslint-config",
    "tsconfig": "yarn workspace @internal/tsconfig"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "turbo": "^1.6.1",
    "@internal/eslint-config": "*"
  },
  "engines": {
    "yarn": "^1"
  }
}
