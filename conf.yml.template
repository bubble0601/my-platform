# Server settings
server:
  ssl: false

# Log settings
log:
  output: deploy/log/output.log
  error: deploy/log/error.log

# MySQL
mysql:
  user: root
  password: password
  host: localhost:3306
  dbname: ibubble

# Session Settings
session:
  name: sid
  # generated by `ruby -e "require 'securerandom';puts SecureRandom.base64(48)"`
  secret: ""
  csrf_secret: ""
